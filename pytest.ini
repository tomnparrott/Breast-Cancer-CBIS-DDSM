[pytest]
testpaths = tests
markers =
    slow: slow tests (single-case inference / gradcam)
    data: tests requiring local dataset files
    quality: quality gates (may fail if experimenting)
    artifacts: artefact checks (files/schema)
addopts = -ra